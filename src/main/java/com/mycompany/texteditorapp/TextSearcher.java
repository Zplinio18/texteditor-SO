package com.mycompany.texteditorapp;

public class TextSearcher {

    // Busca a primeira ocorrência de uma palavra no texto
    public int searchText(String content, String searchText) {
        return content.indexOf(searchText);
    }
}

